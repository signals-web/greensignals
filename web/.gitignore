node_modules
dist
*.local

# Secrets — never commit real env files. `.env.example` is the
# committed placeholder template; everything else is local-only.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Build caches
*.tsbuildinfo
